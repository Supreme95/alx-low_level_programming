#include <stdio.h>

/**
 * _isupper - Check if a letter is in uppercase
 * main: Check the code.
 * int c:The main number to be checked
 *
 * Return: 1 for upper letter or 0 for others
 */

int main(void)
int _isupper(int c)
{
	if (c >= 65 && x <= 90)
	{
		return (1);
	}

	return (0);
}
